/**
 * services/observationService.js
 *
 * Business logic for recording and querying observations against students
 * and classrooms. This layer depends on models/ and storage/, but has no
 * knowledge of the UI or the DOM.
 *
 * Not implemented yet. Expected responsibilities:
 *   - recordObservation(data)
 *   - listObservationsByStudent(studentId)
 *   - listObservationsByClassroom(classroomId)
 *   - deleteObservation(id)
 */

// Intentionally left unimplemented for this milestone.
