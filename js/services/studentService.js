/**
 * services/studentService.js
 *
 * Business logic for creating, updating, listing, and removing students
 * within a classroom. This layer depends on models/ and storage/, but has
 * no knowledge of the UI or the DOM.
 *
 * Not implemented yet. Expected responsibilities:
 *   - createStudent(data)
 *   - getStudentById(id)
 *   - listStudentsByClassroom(classroomId)
 *   - updateStudent(id, changes)
 *   - deleteStudent(id)
 */

// Intentionally left unimplemented for this milestone.
