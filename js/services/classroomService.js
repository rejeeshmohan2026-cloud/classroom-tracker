/**
 * services/classroomService.js
 *
 * Business logic for creating, updating, listing, and removing classrooms.
 * This layer depends on models/ and storage/, but has no knowledge of the
 * UI or the DOM.
 *
 * Not implemented yet. Expected responsibilities:
 *   - createClassroom(data)
 *   - getClassroomById(id)
 *   - listClassrooms()
 *   - updateClassroom(id, changes)
 *   - deleteClassroom(id)
 */

// Intentionally left unimplemented for this milestone.
