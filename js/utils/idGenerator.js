/**
 * utils/idGenerator.js
 *
 * Generates unique identifiers for new records (students, classrooms,
 * observations) before they're persisted.
 *
 * Not implemented yet. Expected responsibilities:
 *   - generateId() — returns a unique string ID, e.g. via crypto.randomUUID()
 */

// Intentionally left unimplemented for this milestone.
