/**
 * models/Classroom.js
 *
 * Describes the shape of a Classroom (cohort/grade/section) record. Models
 * contain no business logic — they exist purely to document and validate
 * the data structures used across the app.
 *
 * Not implemented yet. Expected shape (subject to change):
 *   {
 *     id: string,        // unique identifier (see utils/idGenerator.js)
 *     name: string,      // e.g. "Grade 5 - Section B"
 *     createdAt: string, // ISO date string
 *   }
 */

// Intentionally left unimplemented for this milestone.
