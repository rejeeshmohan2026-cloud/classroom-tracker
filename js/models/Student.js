/**
 * models/Student.js
 *
 * Describes the shape of a Student record. Models contain no business
 * logic — they exist purely to document and validate the data structures
 * used across the app.
 *
 * Not implemented yet. Expected shape (subject to change):
 *   {
 *     id: string,          // unique identifier (see utils/idGenerator.js)
 *     name: string,        // student's name
 *     classroomId: string, // reference to a Classroom record
 *     createdAt: string,   // ISO date string
 *   }
 *
 * Reminder: Student records must never include contact details, government
 * identity numbers, health/medical information, caste or religion data, or
 * photos/videos — see docs/problem-statement.md.
 */

// Intentionally left unimplemented for this milestone.
