# Tests

This folder will hold tests for models, services, storage adapters, and
utilities.

No tests exist yet as part of this milestone. When test work begins,
consider mirroring the `src/js/` folder structure here (e.g.
`tests/services/classroomService.test.js`) so tests are easy to locate
alongside the code they cover.
